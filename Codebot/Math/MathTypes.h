#pragma once
#include <Codebot/ValueType.h>

namespace Codebot
{
namespace Math
{

//  TODO: Add swizzles to vector

/*struct Vec1
{
	Float X;
	Vec1();
	Vec1(Float x);
	operator Float ();
	Vec1& operator = (Vec1 value);
	Vec1& operator += (Vec1 value);
	Vec1& operator -= (Vec1 value);
	Vec1& operator *= (Vec1 value);
	Vec1& operator /= (Vec1 value);
	Vec1 operator + (Vec1 value);
	Vec1 operator - (Vec1 value);
	Vec1 operator * (Vec1 value);
	Vec1 operator / (Vec1 value);
	Boolean operator < (Vec1 value);
	Boolean operator > (Vec1 value);
	Boolean operator == (Vec1 value);
	Boolean operator <= (Vec1 value);
	Boolean operator >= (Vec1 value);
};

struct Vec2
{
	Float X, Y;
	Vec2();
	Vec2(Float x);
	Vec2(Float x, y);
	Vec2& operator = (Vec2 value);
	Vec2& operator += (Vec2 value);
	Vec2& operator -= (Vec2 value);
	Vec2& operator *= (Vec2 value);
	Vec2& operator /= (Vec2 value);
	Vec2 operator + (Vec2 value);
	Vec2 operator - (Vec2 value);
	Vec2 operator * (Vec2 value);
	Vec2 operator / (Vec2 value);
	Boolean operator == (Vec2 value);
};

class Vec3
{
public:
	Float X, Y, Z;
	Vec3();
	Vec3(Float x);
	Vec3(Float x, y, z);
	Vec3& operator = (Vec3 value);
	Vec3& operator += (Vec3 value);
	Vec3& operator -= (Vec3 value);
	Vec3& operator *= (Vec3 value);
	Vec3& operator /= (Vec3 value);
	Vec3 operator + (Vec3 value);
	Vec3 operator - (Vec3 value);
	Vec3 operator * (Vec3 value);
	Vec3 operator / (Vec3 value);
	Boolean operator == (Vec3 value);
};

class Vec4
{
public:
	Float X, Y, Z, W;
	Vec4();
	Vec4(Float x);
	Vec4(Float x, y, z, w);
	Vec4& operator = (Vec4 value);
	Vec4& operator += (Vec4 value);
	Vec4& operator -= (Vec4 value);
	Vec4& operator *= (Vec4 value);
	Vec4& operator /= (Vec4 value);
	Vec4 operator + (Vec4 value);
	Vec4 operator - (Vec4 value);
	Vec4 operator * (Vec4 value);
	Vec4 operator / (Vec4 value);
	Boolean operator == (Vec4 value);
};

typedef Vec2 Point;
typedef Vec3 Vertex;

class Matrix4
{
public:
	Float X, Y, Z, W;
};

class Point
{
public:
	Float X, Y;
};

class Rect
{
public:
	Float X, Y, Width, Height;
};*/

}
}
